%conn = mongo('localhost',27017,'RFID')
[data,str] = xlsread('..\EPC_Hashes.xlsx', 'A:E');
x = cellstr(str(:,1));
scatter3(data(:,2),data(:,3),data(:,4))
len = length(data);

for i = 1:n
en
ls = struct();
for i=1:len
    ls(i).id = str(i+1)
    ls(i).x = data(i, 2)
    ls(i).y = data(i, 3)
    ls(i).z = data(i, 4)
end
strJS = fileread('data.json');
jsonG = jsondecode(strJS);
drawGroup(ls,jsonG);
keySet = [str(2:len+1,1)]
valueSet  = [data(:,2:4)];
mapObj = containers.Map(keySet,valueSet)

